<div dir="rtl" style="text-align: right;">

# 🧰 git — סקריפטי Git בטוחים ומבוקרים עבור Docs-as-System

תיקייה זו מכילה את סט סקריפטי ה־Git הרשמיים של Docs-as-System.  
הסקריפטים נועדו לעבוד בצורה בטוחה, עקבית ושקופה בתוך סביבת הפיתוח המודרנית שבה  
אנשי פיתוח וסוכני AI עובדים יחד באותו מאגר קוד.

כל הסקריפטים בתיקייה אינם עובדים על בסיס משתני סביבה חיצוניים בלבד,  
אלא מסתמכים על קובץ תצורה אחד מרכזי:



קובץ זה מגדיר את כל מדיניות ה־Git בפרויקט, כך שניתן לשנות מדיניות במקום יחיד  
מבלי לגעת בסקריפטים עצמם.

---

## 🎯 מה הסקריפטים האלו פותרים?

בעבודה עם סוכני AI בתוך IDE, יש שני צרכים:

1. **הגנה על מאגר הקוד**  
   הסוכן חייב לעבוד בתוך גבולות ברורים כדי שלא ידרוס ענפים, יבצע push לא רצוי  
   או ייצור PR במצב לא תקין.

2. **אחידות מלאה בין פרויקטים שונים**  
   ההתנהגות של Git חייבת להיות אחידה בכל פרויקט שנבנה לפי Docs-as-System:  
   אותם כללים, אותם נתיבים מותרים, אותו מבנה עם CycleId, אותו תהליך PR.

כתוצאה מכך נוצרה קבוצת סקריפטים קבועה שמטפלת בכל שלבי העבודה:
החל מיצירת ענף חדש ועד למיזוג PR מאושר.

---

## 📁 קבצים בתיקייה זו

| קובץ | תפקיד |
|------|-------|
| **`CREATE_BRANCH.sh`** | יוצר ענף עבודה חדש מתוך הענף הראשי, על פי תבנית הענפים המותרת ורשימת הענפים המוגנים. כולל בדיקות שהעץ נקי, שהשם תקין ושהענף אינו מוגן. |
| **`STAGE_AND_COMMIT.sh`** | מקדם רק את הנתיבים המותרים (כמו `docs/**` ו־`src/**`) ויוצר commit מתויג עם CycleId וטריילרים בהתאם למדיניות הקומיט המוגדרת ב־YAML. |
| **`PUSH_BRANCH.sh`** | דוחף ענף ל־origin, רק כאשר `allowAgentPush=true` במדיניות. מונע דחיפה לענפים מוגנים. |
| **`OPEN_PULL_REQUEST.sh`** | פותח Pull Request במצב טיוטה דרך `gh`, כולל בדיקות תקינות: שאין PR קיים, שיש upstream, שהענף אינו מוגן ושהמדיניות מאפשרת פעולה זו. |
| **`MERGE_AFTER_APPROVAL.sh`** | מבצע Merge ל־PR רק על ידי אדם, ולא על ידי הסוכן. בודק שהבדיקות ירוקות, שה־PR פתוח ושהענף הבסיסי מוגן. |

כל קובץ קטן, מדויק, ומבצע פעולה אחת בלבד בצורה בטוחה.

---

## 🧩 טעינת מדיניות אחידה עבור כל הסקריפטים

כל סקריפט Git משתמש בבלוק טעינה אחיד:

```bash
CONFIG_FILE="docs/agent/AGENT_CONFIG.yaml"

command -v yq >/dev/null 2>&1 || fail "yq is required"

DEFAULT_BRANCH="$(yq '.git.defaultBranch' "$CONFIG_FILE")"
ALLOW_AGENT_PUSH="$(yq '.git.allowAgentPush' "$CONFIG_FILE")"
ALLOW_AGENT_COMMIT="$(yq '.git.allowAgentCommit' "$CONFIG_FILE")"

mapfile -t PROTECTED_BRANCHES < <(yq '.git.protectedBranches[]' "$CONFIG_FILE")
mapfile -t ALLOWED_PATHS_ARRAY < <(yq '.git.allowedPaths[]' "$CONFIG_FILE")

```

כך אין הפתעות ואין התנהגות חופשית:  
כל הפעולות מוגדרות במדיניות אחת, ללא קוד כפול וללא תלויות סמויות.

---

## 🔑 המשתנה היחיד שמגיע מבחוץ: `CYCLE_ID`

כל commit חייב להיות מקושר למחזור עבודה ברור, לדוגמה: 

2025_11_13_001


הסקריפטים לא מייצרים אותו.  
הוא בא מהמשימה עצמה — מהאדם, או מהגורם שמפעיל את הסוכן (IDE/Agent).

### דוגמה להפעלה

CYCLE_ID=2025_11_14_002 ./STAGE_AND_COMMIT.sh

כך נשמרת עקיבות מלאה בין:

- הקוד שנכתב  
- קובצי ה־log  
- סיכומי המחזור  
- הבדיקות  
- ה־PR  
- כל תיעוד נוסף של Docs-as-System  

---

## 📋 דרישות להפעלה

כדי שהסקריפטים יעבדו כראוי נדרש:

- Git מותקן ומוגדר  
- התקנת `yq` לקריאת YAML  
- GitHub CLI (`gh`) לממשק עם PR  
- כניסה ל־GitHub דרך `gh auth login`  
- הרשאות תואמות לפי:  
  - `AGENT_OPERATIONAL_POLICY.md`  
  - `HUMAN_OPERATIONAL_POLICY.md`  

---

## 🛡️ עקרונות בטיחות מחייבים

הסקריפטים נכתבו עם שכבות בטיחות ברורות.

### הגנות על המאגר

- אין יצירת ענף בשם מוגן  
- אין דחיפה ללא `allowAgentPush=true`  
- אין commit ללא `allowAgentCommit=true`  
- אין כתיבה מחוץ ל־`allowedPaths`  
- אין מחיקת היסטוריה  
- אין force-push מצד הסוכן  

### הגנות על תהליך העבודה

- כל PR נפתח במצב טיוטה  
- Merge מבוצע על ידי אדם בלבד  
- בדיקות ירוקות הן תנאי למיזוג  
- כל commit כולל `CYCLE_ID`  
- כל פעולה נרשמת ב־`IMPLEMENTATION_LOG`  

### אחריות הסוכן

הסוכן מבצע פעולות מבוקרות בלבד,  
והאדם שומר על השליטה בשלבים הרגישים: Push, Merge, Approval.

---

© 2025 Tomer Kedem  
Part of the official Docs-as-System template suite.


</div>